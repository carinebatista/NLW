module.exports = async function(db, {proffyValue, classValue, classScheduleValue}){
    // inserir dados na table de teachers

        // ` é chamada de template Literals em JavaScipt
    const insertedProffy = await db.run(`
    
    
    `)
   
}